import 'package:flutter/material.dart';
import 'components/body.dart';

class ConveyerScreen extends StatelessWidget {
  static String routeName = '/conveyer-screen';
  const ConveyerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Body();
  }
}
